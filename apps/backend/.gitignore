/dist
.env
.DS_Store
/uploads
/node_modules
yarn-error.log

.idea

coverage

!src/**

./tsconfig.tsbuildinfo
medusa-db.sql
build
.cache

.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

.medusa

# Generated Medusa admin dashboard build output — must be rebuilt on every
# deploy, never committed. A stale committed copy here silently shadows the
# freshly built admin, hiding any custom widgets/routes added after the copy
# was committed.
/public/admin